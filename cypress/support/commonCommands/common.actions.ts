// Cypress.Commands.add('HandleSuccessAlert', () => {
//     cy.on('window:alert', (message) => {
//         expect([Customer added successfully with customer id :6');
        
//     })
// })

// Cypress.Commands.add('typeAText', (field, text) => {
//     cy.get(field).should('exist').type(text);
// })